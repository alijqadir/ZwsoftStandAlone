Options +FollowSymLinks
RewriteEngine On

# Serve extensionless URLs for existing .html files (e.g., /zwcad-cost -> /zwcad-cost.html)
RewriteCond %{REQUEST_FILENAME} !-d
RewriteCond %{REQUEST_FILENAME}\.html -f
RewriteRule ^(.+?)$ $1.html [L]
